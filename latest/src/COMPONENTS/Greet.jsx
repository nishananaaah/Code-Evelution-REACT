import React from 'react'

function Greet() {
  return (
    <div>
      <h1>Functional Component</h1>
    </div>
  )
}

export default Greet // using export default we can import with any name
// instead we use export only we give the exact name while importing
